# 给其他AI的标准提需求模板

可以直接把下面这段发给别的 AI：

---

请按这个 skill 完整执行，不要改 prompts：

`/Users/siya/Desktop/全量明细与需修改全流程_skill_20260428_0328-0426/skill/SKILL.md`

任务是：

1. 读取两个账号近30天数据表、信息维护表、视频ID-OSS映射表
2. 先做全量数据整理和视频级汇总
3. 输出：
   - 总体明细表
   - 看板数据表
   - 历史跑量爆款
   - 可复制
   - 需修改
   - 直接放弃
4. 对 `需修改` 素材补充：
   - 对标1 / 对标2
   - 数据定位问题
   - 具体修改建议
   - gemini优化后完整脚本建议
   - gemini优化后完整脚本建议2（微润色版）
5. 为 `需修改` 素材生成 PDF
6. 我再给你 `pdf_url` 映射，你把 `pdf_url` 回填到：
   - 需修改表
   - 最终整表

固定要求：

- 不要私自改 prompt
- 对标视频可以从 `历史跑量爆款` 和 `可复制` 里找
- 对标时可以借话术、心理、结构、转化逻辑，也可以参考强句式
- 但不能改掉原视频主菜
- 历史跑量爆款和可复制要保留 `asr`
- 直接放弃不跑 `asr`

---
